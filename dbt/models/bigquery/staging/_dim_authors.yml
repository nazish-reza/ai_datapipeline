```yaml
version: 2

models:
  - name: dim_authors
    description: "Dimension table containing unique authors derived from the books table."
    columns:
      - name: author_id
        description: "Surrogate key generated via dense_rank over cleaned author names"
        data_tests:
          - not_null
          - unique
      - name: author_name
        description: "Cleaned and deduplicated author name from Book-Author column"
        data_tests: []
```