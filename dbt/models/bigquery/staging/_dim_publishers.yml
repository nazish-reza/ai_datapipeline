```yaml
version: 2

models:
  - name: dim_publishers
    description: "Dimension table for publishers with surrogate keys"
    columns:
      - name: publisher_id
        description: "Surrogate key for publishers"
        data_tests:
          - not_null
          - unique
      - name: publisher_name
        description: "Distinct publisher names with whitespace trimmed"
        data_tests: []
```