```yaml
version: 2

models:
  - name: dim_publishers
    description: "Dimension table containing publisher information"
    columns:
      - name: publisher_id
        description: "Surrogate key for publishers"
        data_tests:
          - not_null
          - unique
      - name: publisher_name
        description: "Name of the publisher"
```